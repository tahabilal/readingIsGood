spring:
  datasource:
    generate-unique-name: false
    name: readingisgood
    username: username
    password: password
  h2:
    console:
      enabled: true
  jpa:
    defer-datasource-initialization: true
   # show-sql: true
   # properties:
   #   hibernate:
   #     format_sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
jwt:
  secret: readingisgood
